@model IEnumerable<ASM2.Models.Product>
@using ASM2.IServices;
@using ASM2.Services;
@{
	ViewData["Title"] = "ShowAll";
	Layout = "_Layout";

}
@{
	ProductService _product = new ProductService();
	UserService _User = new UserService();
	List<Product> Lstprod = new List<Product>();
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">

@*
<div class="input-group" style="width:400px;margin:auto;margin-bottom:30px">
	<input type="search" class="form-control rounded" placeholder="Search" aria-label="Search" aria-describedby="search-addon" />
	<button type="button" class="btn btn-outline-primary">Search</button>
</div>
*@
@*@if(Html.TextBox("Search") == null)
{
	Lstprod = _product.GetAllProducts();
}
else
{
	Lstprod = _product.GetProductByName(Html.TextBox("Search"))
}*@
@using (Html.BeginForm("ShowAll", "Home", FormMethod.Get))
{
	@Html.TextBox("search")
	<input type ="submit" value="Search"></input>


}
<table class="table">
	<thead>
		<tr>
			<th style="display:none">
				@Html.DisplayNameFor(model => model.Id)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Name)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Price)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.AvailableQuantity)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Status)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Color)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Size)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Supplier)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Description)
			</th>
			<th style="text-align:center">
				Function
			</th>
		</tr>
	</thead>

	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td style="display:none">
					@Html.DisplayFor(modelItem => item.Id)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Name)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Price)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.AvailableQuantity)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Status)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Color)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Size)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Supplier)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Description)
				</td>
				<td>
					@Html.ActionLink("Edit", "Edit", new {  id = item.Id},new{ @class = "btn btn-primary btn-large"})
					@Html.ActionLink("Details", "Details", new {  id = item.Id  },new{ @class = "btn btn-primary btn-large"})
					@Html.ActionLink("Delete", "Delete", new {  id = item.Id  },new{ @class = "btn btn-primary btn-large"})
				</td>
			</tr>
		}
	</tbody>
</table>

<p>
	<a asp-action="Create" class="btn btn-success">Create New</a>
</p>


